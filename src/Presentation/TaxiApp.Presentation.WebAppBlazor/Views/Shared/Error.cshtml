@model ErrorViewModel

@{
    ViewData["Title"] = "Error";
}

<div class="bg-red-100 border border-red-400 text-red-700 px-4 py-3 rounded relative mt-4 max-w-2xl mx-auto">
    <strong class="font-bold">¡Error!</strong>
    <span class="block sm:inline">@Model.Message</span>
    <div class="mt-4">
        <a href="javascript:history.back()" class="text-blue-600 hover:underline">Volver atrás</a>
    </div>
</div>